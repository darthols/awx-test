---
# defaults file for roles/onet_certificat_generate

# @var key_name: {{ ansible_facts.ansible_hostname }}.pem # Nom de la clé privée.
key_name: "{{ ansible_hostname }}.pem"

# @var crt_name: {{ ansible_facts.ansible_hostname }}.crt # Nom du certificat.
crt_name: "{{ ansible_facts.ansible_hostname }}.crt"
